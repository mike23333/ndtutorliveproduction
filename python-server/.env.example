# Google Cloud Configuration
GCP_PROJECT_ID=your-project-id
GCP_LOCATION=us-central1

# Server Configuration
PORT=8080
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Firebase Configuration (required for local dev)
# Download from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json

# Gemini Model and Voice (native audio for Live API)
# December 2025 version with improved function calling
GEMINI_MODEL=gemini-2.5-flash-native-audio-preview-12-2025
GEMINI_VOICE=Aoede

# Weekly Review Scheduler
# Secret token required by POST /api/review/generate-batch endpoint
# Set this to a secure random string and use the same value in your scheduler config
SCHEDULER_SECRET=your-secure-scheduler-secret
